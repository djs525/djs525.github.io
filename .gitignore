node_modules/
dist/
.DS_Store
*.local
.vite/

# Impeccable local settings and decision-page session state
.claude/settings.local.json
.impeccable/questions/

# TypeScript incremental build cache
*.tsbuildinfo

# Personal documents — this repo is published to djs525.github.io.
# The transcript carries a student number; every resume carries a phone
# number and home city. Nothing here should enter public git history.
*.pdf

# React practice work. Kept on disk next to the real code because that is
# where it is convenient to work on it, but it is not part of the published
# site and does not belong in a repo recruiters open.
learning/
src/**/exercise-*

# Worker: local build state and local secrets. node_modules/ above already
# covers worker/node_modules at any depth.
worker/.wrangler/
worker/.dev.vars
